import { ModValRequest } from './mod-val-request';

describe('ModValRequest', () => {
  it('should create an instance', () => {
    expect(new ModValRequest('', '', '', '', '', '', '', '', '', '')).toBeTruthy();
  });
});
